# Sample environment variables for Render.com deployment
# Copy these to your environment variables in Render dashboard

# Django settings
SECRET_KEY=generate_a_new_secret_key_here
DJANGO_SETTINGS_MODULE=student_management_system.settings_production

# Database URL (if using external database)
# DATABASE_URL=postgres://user:password@host:port/database
